# Hosted cookie acceptance hold

Approved `707fd176611c68b66ac97308c5196018652b7da2` was fast-forwarded to main and published on September 20, 2026 to `orionshop-dashboard` in the existing `orion` Netlify workspace, on Free. No plan purchase occurred. Netlify used Next.js Runtime 5.16.0. Next.js compilation, Functions/Edge packaging and publication completed after the documented public-value scan exception.

Hosted password login, authenticated dashboard rendering, both account freshness endpoints, foreign-scope denial, anonymous page/API denial, invalid callback/confirmation handling and logout passed. The dashboard disclosed awaiting Finance publication, missing costs and historical inventory gaps. Acceptance then stopped: the Auth session Set-Cookie header lacked Secure and HttpOnly, while the containment cookie had both. A successful local build had not detected this production cookie defect. Reports, exports and full hosted acceptance are not certified.

The forward fix adds one shared cookie policy to the server Auth client and middleware refresh. It does not change identities, claims, credentials, accounting, worker behavior or business data. Existing browser Auth factories have no application consumers. Local Supabase rehearsal verifies protected cookies through invite confirmation, password login, refresh and logout, plus viewer denial, membership removal and password-origin guards. The production build and TypeScript pass; browser-secret scanning covers 158 assets with no private marker matches. Authorization and worker-readiness regressions remain release checks.

Safe hold: Netlify automatic publication is locked and the project is disabled, preserving its configuration and deployment. Supabase Auth was restored to its exact previous state: Site URL `http://localhost:3000`, no redirect entries. Production public-table fingerprints matched the pre-acceptance baseline. No worker dispatch, recurring activation, stock read cutover, invitation, schema migration, credential rotation or V4 change occurred.

The commit containing this fix requires explicit approval before main/publication. After approval, verify the exact SHA and worker-off guard, publish only that SHA with the project under controlled hold, reapply the recorded exact hosted Auth origin/callback and intentional local redirects, then repeat HTTPS cookie/refresh acceptance and complete remaining dashboard/report/export/security checks. Do not treat this hold as permission to deploy or invite a beta user.
